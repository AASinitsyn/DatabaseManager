# Database Manager - HTMX Version

Облегченная версия Database Manager на HTMX + Vanilla JavaScript без зависимостей от фреймворков.

## Особенности

- ✅ **Без сборки** - работает сразу после открытия в браузере
- ✅ **Без npm/node** - все зависимости загружаются из CDN
- ✅ **Легковесный** - всего 2 файла (HTML + JS)
- ✅ **Локальное хранилище** - данные сохраняются в localStorage
- ✅ **Полная функциональность** - все возможности React-версии

## Быстрый старт

### Вариант 1: Просто откройте в браузере

1. Откройте файл `index.html` в браузере
2. Готово! Приложение работает

### Вариант 2: С локальным сервером (рекомендуется)

Для лучшей работы рекомендуется использовать локальный веб-сервер:

**Python 3:**
```bash
cd htmx
python -m http.server 8000
```

**Python 2:**
```bash
cd htmx
python -m SimpleHTTPServer 8000
```

**Node.js (npx):**
```bash
cd htmx
npx serve
```

**PHP:**
```bash
cd htmx
php -S localhost:8000
```

Затем откройте браузер по адресу `http://localhost:8000`

## Структура файлов

```
htmx/
├── index.html    # Главный HTML файл со всей разметкой
├── app.js        # Вся логика приложения на Vanilla JS
└── README.md     # Этот файл
```

## Используемые библиотеки (CDN)

- **HTMX 1.9.10** - для динамических взаимодействий
- **Tailwind CSS** - для стилизации
- **Toastify.js** - для уведомлений

Все библиотеки загружаются из CDN, установка не требуется.

## Возможности

### ✨ Управление подключениями
- Создание новых подключений к БД
- Переключение между подключениями
- Удаление подключений
- Симуляция состояния подключения/отключения

### 📊 Редактор запросов
- Выполнение SQL/NoSQL запросов
- Отображение результатов в виде таблицы
- Примеры запросов для каждого типа БД
- Копирование запросов
- Экспорт результатов

### 👥 Управление пользователями
- Создание пользователей
- Настройка прав доступа
- Предпросмотр SQL-команд
- Специфичные права для каждой СУБД

### 🗂️ Управление таблицами
- Визуальный конструктор таблиц
- Настройка колонок и их типов
- Primary Key, Nullable, Unique
- Предпросмотр SQL CREATE TABLE

### 💾 Управление базами данных
- Создание новых БД/Keyspaces/Namespaces
- Настройка параметров (encoding, locale и т.д.)
- Учет специфики каждой СУБД

## Поддерживаемые базы данных

- ✅ PostgreSQL
- ✅ Elasticsearch
- ✅ Aerospike
- ✅ ClickHouse
- ✅ MongoDB
- ✅ Cassandra

## Хранение данных

Все подключения сохраняются в `localStorage` браузера. Данные остаются даже после перезагрузки страницы.

## Интеграция с Backend

Для подключения к реальному backend:

1. Откройте `app.js`
2. Найдите mock-функции:
   - `executeQuery()` - выполнение запросов
   - Обработчики форм в `setupUsersTab()`, `setupTablesTab()`, `setupDatabasesTab()`

3. Замените на реальные API вызовы:

```javascript
async function executeQuery() {
    const query = document.getElementById('query-input').value;
    
    const response = await fetch('http://your-backend/api/query', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
            query: query,
            connectionId: selectedConnection.id
        })
    });
    
    const result = await response.json();
    displayQueryResults(result);
}
```

## Преимущества HTMX-версии

1. **Нет сборки** - не нужен Webpack, Vite и т.д.
2. **Простота** - всего 2 файла
3. **Скорость разработки** - изменения видны мгновенно
4. **Легкость** - минимальный размер приложения
5. **Доступность** - работает везде, где есть браузер

## Отличия от React-версии

- Нет виртуального DOM
- Прямая работа с DOM через Vanilla JS
- Состояние хранится в обычных переменных
- Меньше абстракций, более прямой код

## Troubleshooting

### Проблема: Приложение не загружается
**Решение:** Используйте локальный веб-сервер вместо прямого открытия файла

### Проблема: Данные не сохраняются
**Решение:** Проверьте, что localStorage не заблокирован в настройках браузера

### Проблема: HTMX не работает
**Решение:** Проверьте подключение к интернету (библиотеки загружаются из CDN)

## Лицензия

MIT
